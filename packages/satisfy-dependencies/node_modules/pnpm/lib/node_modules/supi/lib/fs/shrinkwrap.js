"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const encodeRegistry = require("encode-registry");
function pkgIdToRef(pkgId, pkgName, resolution, standardRegistry) {
    if (resolution.type)
        return pkgId;
    const registryName = encodeRegistry(standardRegistry);
    if (pkgId.startsWith(`${registryName}/`)) {
        const ref = pkgId.replace(`${registryName}/${pkgName}/`, '');
        if (ref.indexOf('/') === -1)
            return ref;
        return pkgId.replace(`${registryName}/`, '/');
    }
    return pkgId;
}
exports.pkgIdToRef = pkgIdToRef;
//# sourceMappingURL=shrinkwrap.js.map